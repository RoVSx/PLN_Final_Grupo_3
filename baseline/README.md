# Baselines

Esta carpeta agrupa versiones anteriores del proyecto que se conservaron solo como referencia:

- `clasificador_reclamos_similitud.py`: clasificador zero-shot por embeddings y similitud coseno
- `legacy_naive_bayes/`: baseline inicial previo al enfoque con `sentence-transformers`

La version oficial del proyecto esta en la raiz del repo:

- `entrenar_reclamos.py`
- `clasificar_reclamos.py`
